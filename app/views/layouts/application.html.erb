<!DOCTYPE html>
<html>
  <head>
    <title>Todo</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container">
      <div class="span12">
	<%- if user_signed_in? %>
	  <div class="span4 pull-right">Welcome <strong><%= current_user.email %></strong> <%= link_to"logout", destroy_user_session_path, :method => :delete %> </div>
	<% end %>
	  
	<%= yield %>
      </div>
    </div>
    
  </body>
</html>
